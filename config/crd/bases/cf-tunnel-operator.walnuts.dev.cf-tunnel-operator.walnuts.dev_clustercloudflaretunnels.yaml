---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: clustercloudflaretunnels.cf-tunnel-operator.walnuts.dev.cf-tunnel-operator.walnuts.dev
spec:
  group: cf-tunnel-operator.walnuts.dev.cf-tunnel-operator.walnuts.dev
  names:
    kind: ClusterCloudflareTunnel
    listKind: ClusterCloudflareTunnelList
    plural: clustercloudflaretunnels
    singular: clustercloudflaretunnel
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: ClusterCloudflareTunnel is the Schema for the clustercloudflaretunnels
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClusterCloudflareTunnelSpec defines the desired state of
              ClusterCloudflareTunnel.
            properties:
              foo:
                description: Foo is an example field of ClusterCloudflareTunnel. Edit
                  clustercloudflaretunnel_types.go to remove/update
                type: string
            type: object
          status:
            description: ClusterCloudflareTunnelStatus defines the observed state
              of ClusterCloudflareTunnel.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
